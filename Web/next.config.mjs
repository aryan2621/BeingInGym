/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com', 'github.com', 'i.ytimg.com', 'maps.googleapis.com'],
    },
};

export default nextConfig;
